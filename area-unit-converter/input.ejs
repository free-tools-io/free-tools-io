<form id='tool-input-form'>
</form>
<script type="text/javascript">
    $('#tool-input-form').jsonForm({
      schema: {
        value: {
            type: 'number',
            title: 'Value',
            required: true,
            maxLength: 10000,
            default: "<%= query.value ? query.value : '' %>"
        },
        from: {
            type: "string",
            title: "From Unit",
            enum: [ "mm2", "cm2", "m2", "ha", "km2", "in2", "ft2", "ac", "mi2" ],
            default: "<%= query.from ? query.from : 'cm2' %>"
        },
        to: {
            type: "string",
            title: "To Unit",
            enum: [ "mm2", "cm2", "m2", "ha", "km2", "in2", "ft2", "ac", "mi2" ],
            default: "<%= query.to ? query.to : 'in2' %>"
        }
    },
    form: [
        '*',
        {
            type: "submit",
            title: "Convert"
        }   
    ],
    onSubmit: function (errors, values) {
        var url = window.location.href;
        if(url.indexOf("?") > 0) {
          url = url.substring(0, url.indexOf("?"));
        } 
        url += "?" + jQuery.param(values);
        window.location.replace(url);
      }
    });
</script>
  